# ==================================================
# Medi Backend - Environment Variables Template
# ==================================================
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 변경하세요
# cp .env.example .env

# --------------------------------------------------
# Database Configuration
# --------------------------------------------------
MYSQL_ROOT_PASSWORD=your_secure_root_password
MYSQL_PASSWORD=your_secure_password
MYSQL_DATABASE=medi
MYSQL_USER=admin

# --------------------------------------------------
# Google OAuth2 (YouTube API)
# --------------------------------------------------
# Google Cloud Console에서 발급받은 OAuth2 클라이언트 ID/Secret
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# --------------------------------------------------
# CORS Configuration
# --------------------------------------------------
# 허용할 프론트엔드 도메인 (쉼표로 구분)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,http://localhost:3000

# --------------------------------------------------
# Email Configuration (Gmail SMTP)
# --------------------------------------------------
# Gmail 계정 및 앱 비밀번호
# 앱 비밀번호 생성: https://myaccount.google.com/apppasswords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password

# --------------------------------------------------
# Chatbot API (Optional)
# --------------------------------------------------
# 챗봇 서비스 URL (사용하지 않으면 기본값 사용)
CHATBOT_API_URL=http://chatbot-service:8000

# --------------------------------------------------
# Redis Configuration (Optional Override)
# --------------------------------------------------
# 기본값: redis:6379 (docker-compose 내부 네트워크)
# REDIS_HOST=redis
# REDIS_PORT=6379

# --------------------------------------------------
# Spring Profile
# --------------------------------------------------
# 프로덕션 환경에서는 prod 사용
SPRING_PROFILES_ACTIVE=prod
